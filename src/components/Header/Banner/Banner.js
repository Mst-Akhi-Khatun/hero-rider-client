import React from 'react';
import banner from '../../../images/Banner/banner.jpeg';

const Banner = () => {
    return (
        <div>

            <img src={banner} alt="" />
        </div>
    );
};

export default Banner;